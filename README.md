## フットサルクラブ管理アプリ

トップ / サテライト / U18 の選手・コーチのアカウント、権限をまとめて管理するアプリの土台です。
現在実装済みなのは「①アカウント・権限まわり」のみです（スケジュール・メッセージ・選手証は今後追加予定）。

### セットアップ

```bash
npm install
npx prisma migrate deploy
npm run dev
```

`http://localhost:3000` を開くと、まだ管理者が誰もいない場合は自動的に `/setup` に案内されます。
そこで最初の管理者アカウント（名前・カテゴリー・メール・パスワード）を作成してください。

### できること

- 選手・コーチが自分でアカウント登録（名前・背番号・カテゴリーを入力）
- 管理者は `/admin` からメンバー一覧を確認し、カテゴリー変更・管理者権限の付与/剥奪・アカウント手動追加ができる
- カテゴリーは以下の6種類（`src/lib/categories.ts` で管理）
  - トップ選手 / トップコーチ
  - サテライト選手 / サテライトコーチ
  - U18(U15)選手 / U18コーチ

### 技術構成

- Next.js (App Router) + TypeScript + Tailwind CSS
- Prisma + SQLite（`prisma/schema.prisma`）
- 認証は自前実装（bcryptjsでパスワードハッシュ化、joseでJWTセッションcookie）
